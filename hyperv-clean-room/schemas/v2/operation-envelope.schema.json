{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/rogue-shadowdancer/codex-hyperv-clean-room-plugin/contracts/v2/schemas/operation-envelope.schema.json",
  "title": "Hyper-V clean-room schema-v2 operation envelope",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "ok",
    "operationId",
    "changed",
    "data",
    "warnings",
    "evidencePath"
  ],
  "properties": {
    "schemaVersion": { "const": 2 },
    "ok": { "type": "boolean" },
    "operationId": { "type": "string", "format": "uuid" },
    "changed": { "type": "boolean" },
    "data": { "type": "object", "maxProperties": 64 },
    "warnings": {
      "type": "array",
      "maxItems": 64,
      "items": { "type": "string", "maxLength": 1000 }
    },
    "evidencePath": { "type": ["string", "null"], "maxLength": 32767 },
    "error": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "message"],
      "properties": {
        "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
        "message": { "type": "string", "maxLength": 2000 },
        "details": { "type": "object", "maxProperties": 32 }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "ok": { "const": false } },
        "required": ["ok"]
      },
      "then": { "required": ["error"] },
      "else": { "not": { "required": ["error"] } }
    }
  ]
}
