# Task handoff: exact Birdsgone Test2 manifest compatibility

`relayProtocolVersion: 1`

`projectPath: E:\study\great_projects\codex-hyperv-clean-room-plugin`

## Objective and authorization

Repair plugin base `0.4.1` so the exact immutable Birdsgone
`v0.1.0-test.2` end-user-complete sidecar validates without rewriting, while
preserving all 20 public tools, closed tool inputs, five schema-v1 bytes, seven
schema-v2 paths/IDs, Plan/Apply, evidence, credentials, and the 31-payload
install topology. Publish through one ordinary protected pull request, install
only the exact merge commit through the repository installer, then execute the
separately authorized Test2 sequence on owned VM
`Birdsgone-W11-HV-20260723` with new credential profile
`birdsgone-w11-test2`.

The production authorization is limited to profile revalidation, guarded VM
start, interactive credential initialization, `inspect_guest`, the declared
non-UI portable smoke, evidence collection/validation, and guarded graceful
shutdown. It does not authorize checkpoint, network, VM-create, deletion,
forced power-off, arbitrary guest command, UI/manual attestation, tag, Release,
direct/force push, branch deletion, ACL/group/elevation change, or another VM,
artifact, profile, or credential name.

- Protected base at task start:
  `d4504fcb28ea8b857be1d9a6449d4ea49e2379c4`.
- Task branch: `codex/v041-test2-manifest-compat`.
- Frozen candidate build: `0.4.1+codex.20260813075830`, generated by the one
  authorized helper invocation and never regenerated.
- This worktree is the unique plugin-repository writer. Birdsgone worktrees and
  their user-owned changes remain read-only.
- Task Mail credential state remains `credential_state_missing`; no predecessor
  identity is reused or impersonated.

## Exact immutable inputs

- GitHub prerelease: `v0.1.0-test.2`, target
  `30f6540dec748feed111faec9604c4be8ef8efc5`.
- ZIP: `Birdsgone_0.1.0_windows-x64-portable.zip`, 344,485,947 bytes,
  SHA-256
  `1a7cd75229c785b0724ea9552439504a8a4721242246ed53c2251594b36f8f6a`.
- Sidecar: `portable-manifest.json`, 142,013 bytes, SHA-256
  `f9267d70ca9b412bc54170489e38d211bd528a28e64a1fe15fa2cf987471650c`.
- Non-UI profile SHA-256
  `37c4c8a49cef9a7b982c842a197f47f54a7cb02c10c3ff1c9043ccf803b31021`.
- The profile declares stage, deploy, launch, running assertion, stop, stopped
  assertion, and bounded stop/assert cleanup. It declares no UI or manual
  assertion.

## Reproduced defect and repair

The previous exact installed build was
`0.4.1+codex.20260805101924` at protected commit
`d4504fcb28ea8b857be1d9a6449d4ea49e2379c4`. Exact Test2 profile validation
failed with `PROFILE_INVALID` / `PORTABLE_MANIFEST_INVALID`: three unsupported
root selected-source fields, 42 rejected string source-input rows, and two
unsupported Maa Agent size fields. No VM, guest, credential, deployment, or
Hyper-V mutation was performed by that failed validation.

The compatible repair is deliberately closed:

- the selected-source triple is absent or complete, exactly
  `fresh-exact-head`, and size/SHA-bound to the unique ordinal-exact removed
  `portable-manifest.json`;
- each source-input inventory is a homogeneous bounded object-identity or safe
  string-path array; string paths are separator-normalized,
  case-insensitively unique and mapped by fixed prefix to the complete ZIP
  inventory; new compatibility fields require canonical schema casing;
- Maa Agent sizes are absent or paired and their path/size/SHA identities match
  the ZIP inventory;
- partial, mixed, unknown, unsafe, colliding, missing, or drifted identities
  remain invalid. All existing ZIP/profile/sidecar, documentation, inventory,
  deployment, entrypoint, and evidence checks remain unchanged.

Both authoritative/runtime schema copies stay byte-identical, and historical
object-valued external provenance remains valid. No tool name, tool schema,
adapter action, or plan semantics changes.

## Verification and current gate

Exact frozen-candidate source verification passed:

- exact immutable Test2 sidecar: Draft 2020-12 schema valid with zero errors;
- exact sidecar/profile native resolution: version `0.1.0`, 370 inventory
  entries, `uiRequired=false`, exact sidecar size/SHA retained;
- Gate 6 contract: seven positive P3.1 fixtures, five schema-invalid fixtures,
  41 inherited negative cases, 20 tools, five v1 and seven v2 schemas, zero real
  operations;
- PowerShell 5.1 Gate 7 runtime: 452 assertions, 20 tools, zero real host,
  Hyper-V, guest, portable, WebDriver, or UI operations.
- install-source: build `0.4.1+codex.20260813075830`, cachebuster
  `20260813075830`, 31 payload paths, five v1 and seven v2 schemas, no untracked
  payload and no reparse point;
- public-release aggregate: all 13 parser, format, plugin/skill, Gate 1, Gate 2,
  documentation, publication-policy/history/contract, and Gate 4 CI-safe checks
  passed; documentation covered 17 documents and 101 local links;
- final exact profile/sidecar native readback retained profile SHA-256
  `37c4c8a49cef9a7b982c842a197f47f54a7cb02c10c3ff1c9043ccf803b31021`,
  sidecar size 142,013 and SHA-256
  `f9267d70ca9b412bc54170489e38d211bd528a28e64a1fe15fa2cf987471650c`,
  370 files, `sourceMode=fresh-exact-head`, and `uiRequired=false`.

Initial PR head `53f05f8cddc2301f141d003cbc0e6bda7892bf61` opened ordinary
PR #37 and both push/pull-request `public-release-validation` runs passed.
Codex review then found two actionable integration gaps: the production
GuestWorker omitted the selected-source triple from its external sidecar
allowlist, and shared schema definitions admitted the Test2 string inventories
and Agent sizes into the historical branch. The additive review fix admits the
three fields in the fixed worker while retaining exact staged sidecar
size/SHA binding, and gives the end-user branch separate provenance definitions.
Dynamic contract probes prove the historical branch still rejects the Test2
alternatives, and an implementation regression scopes the worker allowlist and
its staged-byte binding. Gate 6, complete Gate 7, exact immutable input
readback, and all 13 local public-release checks passed again after the fix;
all real-operation counters remain zero.

The source gate still requires final exact-staged substantive review, one
additive commit/push, review-thread reconciliation, fresh exact-head checks and
review, the full unchanged-head window, and ordinary protected merge. Until
that merge and exact reinstall, the production Test2 sequence is
`notPerformed`. All source-validation real host, Hyper-V mutation, guest,
portable deployment, WebDriver, and UI operation counts are zero.

Historical H4/G9 installed-copy acceptance through `validate-gate4.ps1` is not
source evidence and is not silently substituted here. That script includes a
production host/missing-ISO plan path outside the exact Test2 operation list;
it remains `notPerformed` unless separately authorized. Post-merge install
acceptance instead uses exact source/install hashes plus catalog-only and
explicit child-local mock checks before the specifically authorized Test2
tools.

## Next ordered gates

1. Preserve frozen build `0.4.1+codex.20260813075830` unchanged; never rerun the
   cachebuster helper.
2. Commit and push the reviewed additive fix on the existing PR #37, reply to
   and resolve only the two addressed review threads, then require exact-head
   hosted checks, a fresh Codex review with zero actionable findings, and a
   fresh unchanged-head 30-minute window before the ordinary protected merge.
   Hosted infrastructure unavailability may be recorded only as
   `waived_non_code` / `notPerformed` after exact local equivalents pass; do
   not claim hosted success or silently bypass protection.
3. After ordinary protected merge, validate the exact protected source, run
   `check_install.ps1`, install once through `install_plugin.ps1`, then require
   exact 31-file/source/build/cachebuster/marketplace/enabled/catalog readback.
4. From freshly loaded repaired plugin bytes, revalidate the unchanged Test2
   profile. Reinspect the named VM, review and consume one start plan, then run
   the interactive two-credential initializer. Never expose secret content.
5. Run `inspect_guest`, the exact non-UI profile, collect evidence to a safe
   non-repository output root, validate exported `evidence.json`, then review
   and consume one graceful-shutdown plan and verify Off.

`blockers: []`
